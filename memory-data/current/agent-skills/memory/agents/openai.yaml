interface:
  display_name: "Total Memory"
  short_description: "Persistent cross-session memory and self-improvement"

policy:
  allow_implicit_invocation: true

dependencies:
  tools:
    - type: "mcp"
      value: "memory"
      description: "Total Memory MCP server — 32 tools for persistent knowledge storage, 4-tier search, knowledge graph, episodic memory, skill tracking, self-improving agent, privacy stripping, branch-aware context, and cognitive engine"
