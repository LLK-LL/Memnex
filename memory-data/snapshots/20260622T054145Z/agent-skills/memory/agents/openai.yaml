interface:
  display_name: "Total Memory"
# REDACTED: sensitive-looking memory line

policy:
  allow_implicit_invocation: true

dependencies:
  tools:
    - type: "mcp"
      value: "memory"
      description: "Total Memory MCP server 鈥?32 tools for persistent knowledge storage, 4-tier search, knowledge graph, episodic memory, skill tracking, self-improving agent, privacy stripping, branch-aware context, and cognitive engine"
